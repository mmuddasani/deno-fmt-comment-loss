// before-value (context: top-level)
[
  // before-value (context: array spot 0)
  "v0"
  // after-value (context: array spot 0)
  ,
  // before-value (context: array spot 1)
]
// after-value (context: top-level)
